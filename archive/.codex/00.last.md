## 做了什麼

- 在獨立分支 `chore/archive-legacy-hackathon` rebase 到最新主線 `6267403`，沒有衝突。
- 將 `shop/`、`console/`、`stubs/`、`control/BRIEF.md` 與 `control/tasks/` 共 34 個檔案移到 `archive/legacy-hackathon/`，保留內容與權限；每個檔案自初始化 `e12599a` 起均無變更，亦與原工作區一致。
- 新增 `archive/README.md` 的封存理由與還原說明，保存舊根目錄 README，更新根目錄與 control README 的入口和連結。
- 假設：這批未修改的初始化任務及其模擬服務可作歷史資料封存。舊編號任務流程需先還原路徑才能使用；封存中的歷史指令不當作現行啟動入口。
- 保留最新主線的新後端骨架、`shop-web/docs/`、現行程式、契約與 gate；原工作區的未提交改動未納入本分支。

## 依據什麼驗證的

- `git fetch origin master` 抓到 `6267403`；`git rebase origin/master` 輸出 `Successfully rebased`。
- `git diff --exit-code e12599a HEAD -- shop console stubs control/BRIEF.md control/tasks` 沒有差異。
- 執行 Python 逐檔雜湊、權限與 Markdown 連結檢查：34 個封存檔案完全一致、108 個其他原有檔案保持一致、21 個本機文件連結有效；舊根目錄 README 完整保留。
- `git diff --check` 通過。
- `docker compose -p nightwatch-shop-web-archive-check -f compose.yaml config --quiet` exit 0；只出現原有 `version` 欄位過時警告。
- 在此分支執行既有虛擬環境的 `python -B -m nightwatch_agent --help`，exit 0 並列出 CLI 用法。沒有啟動背景程序。

## 沒有驗證的

- 未重建或啟動購物網站、未做瀏覽器或 live 調查驗證。本機上一輪已確認沒有該網站的正式映像，8080 也沒有服務；本輪只驗證設定與程式檔案未改動。
- 未執行已封存的舊任務、模擬服務或其歷史驗收指令；舊文件本身提及的缺少實作與外部服務仍然存在。
- 未執行會改動 git 或自動合併的舊 task/gate 提交流程。

## 契約疑問

無
