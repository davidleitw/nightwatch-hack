# console 03:舞台頁 I(講者用):固定摘要 + 服務圖

## 為什麼要有這段
觀眾任何時刻看向投影,五秒內要能回答:現在發生什麼、NightWatch 知道什麼、接下來誰要做什麼。版面固定不跳動,這段做上面兩列與左欄的服務圖。

## 目標
- 最上面兩列永遠在:目前階段的中文標題與「偵測後 mm:ss」;第二列依階段換內容(模型最近一句、待批准的提案、驗證窗進度、已恢復加報告連結);第三列是 AI 主張一句、Go 稽核 n/5、作用範圍,右側留批准與中止兩顆按鈕的位置(這段只畫灰殼)。
- 服務圖 13 個節點固定格子、一輪內不重排;健康與判定是兩個維度,分開畫且都有文字,不能只靠顏色;每個節點顯示主軸的值與趨勢,非服務節點形狀不同;沒數字的節點畫灰,不能因為缺欄位掛掉。
- 「正在查 X」的標記:模型正在查哪個節點就標哪個,查完撤掉。
- 圖下方一條合成顧客的結帳失敗比曲線,隨每張快照累積。
- 底部顯示三個資料來源的健康與 Jaeger 連結。
- 投影可讀:主句、活動流主文、節點名的字級下限照 `BRIEF.md`。

## 契約在哪
- `../contracts/API.md` §1 status 與 assessment 兩個維度、§4 capabilities 的版面、§5 節點欄位與主軸與趨勢、§6 read model 的階段與提案與主張、§7 稽核
- `../contracts/schemas/{snapshot,node,edge}.schema.json`、`../contracts/fixtures/catalog_pool_leak/snapshots.jsonl`

## 怎麼算做完
- 對假 control 十倍速播放:從偵測到已恢復,節點顏色會變、元凶被標出、每個階段的標題都出現過。
- `?fixture=catalog_pool_leak` 跳到排查中、等待批准、已恢復三個秒數(以 `events.jsonl` 為準),第二列內容與「正在查」標記各自對。
- 1920×1080 與 1440×900 各截一次,量到的字級達到 `BRIEF.md` 的下限。
- 截圖存 `.shots/03-stage-investigating.png`、`.shots/03-stage-awaiting.png`、`.shots/03-stage-1440.png`。

## 測試
只寫契約邊界與主線:階段 → 標題與第二列的對應;「正在查」節點的推導;節點文字的換算(主軸有值、主軸為 null 各一)。

## 不做什麼
活動流、批准與中止的真功能、報告頁。

## 接線點
接 1:對真 control 02+03(假 stack)開舞台頁,13 個節點有顏色、8 條邊有數字、底部三個來源 ok;對不上的列給協調者。
