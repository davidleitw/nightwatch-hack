## 做了什麼

- 在獨立 worktree 的 `feat/agent-repair-loop` 完成修改，僅涉及 `control/`。
- 新增修復 adapter、API schema 副本與三個工具：查演練故障、解除已觀測的同一故障、查店面健康。設定 `NIGHTWATCH_SHOP_URL` 才啟用；每件調查最多嘗試一次 DELETE，保存前後狀態與可見錯誤。
- 更新 graph／loop／prompt／報告說明、README、環境範例與 `control/SYSTEM_DESIGN.md`；新增真實 HTTP／模型驗證腳本。usage 保存逐次模型請求與 cache token 命中率。
- 假設：使用者所指 API 是店面現有 `/api/demo-faults`；本機演練由單一操作人使用。依此次明確要求建立 worktree、使用真模型與提交 MR。驗證新增兩筆本機 demo 訂單。

## 依據什麼驗證的

- `PYTHONPATH=control /Users/davidlei/Desktop/nightwatch-hack/control/.venv/bin/python -m unittest discover -s control/tests -q`：23 tests，OK；模型單元測試使用既有替身。
- 以 `uv run --no-project --python <現有 server Python> --offline --env-file <現有 .env>` 執行 `control/acceptance/demo_repair.py --shop-url http://127.0.0.1:8005 --graph-url http://127.0.0.1:8004/api/graph --output <control/.codex/repair-smoke-3>`：真模型 gpt-6-astra 經 POST 開案，兩輪皆完成並保存完整 export／模型報告。
- checkout_exception、database_write_lock 皆為結帳 500 → agent DELETE → 同一購物車結帳 201；解除前租期分別剩 49.950、40.388 秒。報告 outcome=unresolved，明確保留根因與業務觀測不足；成功結帳由驗證腳本在模型結案後獨立確認。
- 修復 cache：68,272/79,020＝86.40%、38,558/50,508＝76.34%，合併 82.48%。另實跑原版 master loop：173,535/190,729＝90.99%。逐輪 token 加總與 export usage 相符；不同回合數／觀測量，不能宣稱與原版同水準。
- 使用本機既有 cache 的 `hatchling.build.build_wheel('dist')` 離線建出 wheel；包含修復模組與 schema。由 wheel 執行真實 graph／fault／health 查詢，錯誤身份在 DELETE 前被拒絕，未設環境變數時仍唯讀。
- `python -m compileall`、`git diff --check` 通過。腳本輸出兩次購物車清理 204、Verification server stopped；`lsof -nP -iTCP:8017 -sTCP:LISTEN` 無監聽。啟動的驗證程序均已結束。
- 初次驗證遇到非 JSON 的店面 500、修改造成的 context import 錯誤，已修正後重跑成功。直接 uv build 因 cache 權限／空快取缺 hatchling 失敗，改用既有快取離線建置成功，未新增依賴。

## 沒有驗證的

- 自動 detector 觸發、checkout_delay 完整模型流程、長期 cache 穩定性、多人並發／程序崩潰時的解除結果未實跑。
- 完整模型修復從 worktree 原始碼啟動；wheel 已實際執行工具，但未再由 wheel 重跑完整模型流程。沒有瀏覽器驗證。
- 解除狀態與健康 API 不等於完整業務恢復；本次只確認兩次專用購物車成功結帳。原版與修復數據不是受控 benchmark。

## 契約疑問

- `/api/demo-faults` 的 DELETE 沒有原子 instance／revision 條件。店面能否提供條件式解除，避免前置 GET 後故障被替換或到期？目前保留原 API，限單一操作人本機演練，並在結果揭露此限制。
