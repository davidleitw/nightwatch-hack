## 做了什麼

- 以 origin/master 建立 feat/backend-api-readiness-mr，已同步至 92a1655（含前端工作台 #19），整合本次 8 份 API／文件修改；既有未提交 agent 修改未納入，原工作區保留。
- 提供真實 state／graph SSE、近期日誌、調查歷史與獨立 report／保存快照 API；盤點與假設詳見 control/API-READINESS-SPEC.md。
- 假設：先同步最新 master，再開 MR 指向 master，不覆蓋本機 master 的未提交工作。

## 依據什麼驗證的

- 本輪 python -m unittest discover -s tests -v：23 項通過。
- 本輪 python -m hatchling build -t wheel，以及 uv pip install --offline --no-deps --python … --target …：建置與安裝成功。
- 以建置的 wheel 啟動 production uvicorn；實際 HTTP 驗證 state schema、3 筆 Monitor 函式日誌、5 種 SSE 事件、保存的 failed 報告、歷史與錯誤碼均通過。驗證程序已停止。
- git merge --ff-only origin/master：由 772183d fast-forward 至 92a1655；git diff --check 無輸出。

## 沒有驗證的

- 本輪未驗證瀏覽器、真實模型、正式 shop、重啟與非空 snapshot 回傳；單元測試的替身不算模型實測。
- 完整實驗報告仍缺注入真相、基線、根因稽核與修復窗口；故障操作尚未接入。

## 契約疑問

- 沒有确认根因時，舊 report schema 必填的根因與 AI 起點應如何表示？
- config 是否提供正式節點 layout？目前假設三欄排列。
- 手動調查要保留舊事故相容投影，或讓前端全面改用 investigations？
