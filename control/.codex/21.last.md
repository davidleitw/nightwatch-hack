## 做了什麼

- 微調 `control/nightwatch_agent/prompts.py`：修復說明須根據原本 root cause 與證據，解釋原因如何造成症狀、處置為何有助改善、預期業務效果及實際驗證結果；對使用者不敘述工具名稱、API、HTTP 方法與參數操作。
- 根因未確認時保留假設，不從故障卡倒推確定根因，也不杜撰修復機制。錯誤與限制仍須白話交代，執行細節保留於 events／evidence。
- 同步更新 `control/SYSTEM_DESIGN.md`。本次從遠端 master 建立獨立 worktree，僅修改 control。假設：這次只微調說明方式，不變更修復操作流程。

## 依據什麼驗證的

- 執行 `PYTHONPATH=control /Users/davidlei/Desktop/nightwatch-hack/control/.venv/bin/python -`，實際讀取本機 8004 graph，呼叫 GraphAPI.prepare 與 static_instructions；確認生成的修復 System Prompt 包含根因解釋、不敘述工具操作、保留不確定性及區分預期／驗證結果四項規則。
- 實際輸出仍有原本七項工具，包含三個修復工具。`git diff --check` 通過。

## 沒有驗證的

- 本次未重新呼叫模型、注入故障或量測 cache；只驗證實際生成的 prompt，不能宣稱模型已產生符合新說法的回答。未啟動驗證服務。

## 契約疑問

無。
